import 'package:flutter/material.dart';
class Mall extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title:Text("小组2"),
      ),
      body:Center(
        child:Text("小组2",style:TextStyle(fontSize: 50)),
      )
    );
  }
}