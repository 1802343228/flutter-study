import 'package:flutter/material.dart';
class Subject extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title:Text("小组4"),
      ),
      body:Center(
        child:Text("小组4",style:TextStyle(fontSize: 50)),
      )
    );
  }
}